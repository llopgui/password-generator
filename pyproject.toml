[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "password-generator"
version = "2.0.0"
description = "Generador de contraseñas memorable con modos secure y diceware"
readme = "README.md"
license = { text = "WTFPL" }
requires-python = ">=3.13"
dependencies = ["pyperclip>=1.9.0"]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "ruff>=0.11.0",
    "mypy>=1.15.0",
]

[project.scripts]
password-generator = "password_generator.ui:run_app"

[tool.setuptools]
packages = ["password_generator", "password_generator.testsuite"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["password_generator/testsuite"]
python_files = ["test_*.py"]

[tool.mypy]
python_version = "3.13"
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_return_any = true
disallow_untyped_defs = true
check_untyped_defs = true
ignore_missing_imports = true
